-- This is a temporary fix fot the mason issue that is occuring, try and remove later on to see if fixed
return {
  { "mason-org/mason.nvim", version = "^1.0.0" },
  { "mason-org/mason-lspconfig.nvim", version = "^1.0.0" },
}
